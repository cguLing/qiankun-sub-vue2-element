// import { models } from '@/settings';

const getters = {
  sidebar: state => state.app.sidebar,
  size: state => state.app.size,
  device: state => state.app.device,
  // email: state => state.user.email,
  // name: state => state.user.name,
  // bussid: state => state.user.bussid,
  // bussname: state => state.user.bussname,
  // admin: state => state.user.admin,
  // roles: state => state.user.roles,
  // token: state => state.user.token,
  // avatar_url: state => state.user.avatar_url,
  // permisaction: state => state.user.permisaction,
  // permission_routes: state => state.permission.routes,
  // service_tree: state => state.service_tree.service_tree,
  // mod: state => state.app.mod,

  // addRoutes: (state) => state.permission.addRoutes,
  // moduleMenu: (state) => state.permission.moduleMenu,
  // activeModule: (state) => state.permission.activeModule,
  // activeModuleMenu: (state) => state.permission.activeModuleMenu,

  // [models.projects]: (state) => state[models.projects],

}
export default getters
